<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Closet Tracker</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    
  <nav>
      <!-- <img src="/images/camera.svg"> -->
      <a href="/closet" <%- title === 'All Closet Items' ? 'class="active"' : '' %>>ALL Closet Items</a>
      <% if (user) { %>
        <a href="/closet/new" <%- title === 'Add Item' ? 'class="active"' : '' %>>ADD Item</a>
        <a><span>Logged in as: </span> <strong><%= user.name %></strong></a> 
        <!-- <img class="user-avatar" src="<%= user.avatar%> " alt="avatar">         -->
        <a href="/logout">LOG OUT</a>
        <% } else { %>
      <a href="/auth/google" class="login">LOG IN <img src="https://i.imgur.com/FHjYyi0.png"></a>
      <% } %>
  </nav>       
  <main>
  
